# ─── ntfy.sh PUSH NOTIFICATION ──────────────────────────────────
# A private topic name — KEEP THIS SECRET (like a password, not guessable)
# SETUP: Install "ntfy" app from Play Store / App Store
#        → Tap "+" → Type this exact topic name → Subscribe
#        → Done! You'll get instant push notifications like WhatsApp
NTFY_TOPIC=ayushman-secret-cards-x7k2

# ─── WHATSAPP NOTIFICATION (CallMeBot — FREE) ────────────────────
# SETUP STEPS (one-time, takes 2 min):
#   1. Add this number to your WhatsApp contacts: +34 644 65 21 91
#   2. Send this EXACT message to them: I allow callmebot to send me messages
#   3. They will reply with your API key in ~30 seconds
#   4. Fill in YOUR WhatsApp number and the API key below:
YOUR_PHONE=+91XXXXXXXXXX
CALLMEBOT_APIKEY=XXXXXXXX

# ─── SELF-DESTRUCT ──────────────────────────────────────────────
# Secret keyword to unlock/reset the destroyed app from dashboard
DESTROY_KEY=dev-unlock-2025

# ─── SERVER ─────────────────────────────────────────────────────
PORT=3001
